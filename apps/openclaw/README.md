# @hawktask/openclaw

OpenClaw config and skills for Hawktask.

- **config/** – Gateway and agent configuration. Workspace path and billing tier.
- **skills/** – Proposal templates and automation skills (OpenClaw plugins).

Local Gateway: run from repo root with `docker-compose up` or `docker run -p 18789:18789 openclaw/gateway:latest`.
